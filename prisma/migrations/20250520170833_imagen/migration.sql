BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Impresora] ALTER COLUMN [imagen] NVARCHAR(1000) NULL;

-- AlterTable
ALTER TABLE [dbo].[Utilizables] ADD [imagen] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
